direnv

Purpose
- Per-directory environment loading.

Usage
- Create .envrc, then run: direnv allow
- Deny: direnv deny
- Reload: direnv reload

Notes
- nix-direnv is enabled for Nix shell integration.
