fzf

Purpose
- Fuzzy finder for files, history, and directories.

Key bindings (Zsh integration)
- Ctrl-t: insert selected file paths
- Ctrl-r: search command history
- Alt-c: cd into a selected directory

Notes
- Default source uses fd with files only.
- Default options include a 40% height and border.
