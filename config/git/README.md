Git Usage

Defaults
- Default branch: main
- Pull rebase enabled
- Git LFS enabled
- Delta enabled with side-by-side view

Aliases
- `br` branch, `co` checkout, `st` status
- `cm` commit -m, `ca` commit --amend
- `dc` diff --cached, `d` diff
- `pl` pull, `ps` push

Full docs: docs/tools/git.md
